# Minimal dependencies for the SVG -> PPTX converter
# ==================================================
#   pip install -r requirements.txt

# SVG -> PPTX core (svg_to_pptx.py)
python-pptx>=0.6.21
XlsxWriter>=3.0.0
# PowerPoint-style Merge Shapes materialization
skia-pathops>=0.9.2
# OpenType shaping + glyph outlines for text operands
uharfbuzz>=0.50.0

# Image inspection / processing (lazy imports in converter and linter)
Pillow>=9.0.0

# Preferred XML engine for source-preserving PPTX mutation (falls back to stdlib)
lxml>=4.9.0

# Optional: YAML frontmatter in template registration / quality reports
PyYAML>=6.0

# Optional: workbook reading in the PPTX -> SVG importer
openpyxl>=3.1.0
